; PlatformIO Project Configuration File
;
;   Build options: build flags, source filter
;   Upload options: custom upload port, speed and extra flags
;   Library options: dependencies, extra library storages
;   Advanced options: extra scripting
;
; Please visit documentation for the other options and examples
; https://docs.platformio.org/page/projectconf.html

[env:m5stick-c]
platform = espressif32
board = m5stick-c
framework = arduino
monitor_speed = 115200
board_build.partitions = huge_app.csv

lib_deps = 
    m5stack/M5Unified @ ^0.2.10
    m5stack/M5GFX @ ^0.2.16

build_flags = 
    -DCORE_DEBUG_LEVEL=0
    -DCONFIG_HTTPD_WS_SUPPORT

extra_scripts = pre:scripts/gzip_html.py

lib_ldf_mode = deep+

lib_ignore =
    WebServer

; [env:native] disabled — requires MinGW (g++) which is not available on this machine.
; All tests run on-device via the test_embedded environment (m5stick-c).